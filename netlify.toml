[build]
  publish = "."
  command = "echo 'No build step needed for static HTML'"

[[redirects]]
  from = "/"
  to = "/sketchflow.html"
  status = 200

[context.production.environment]
  NODE_ENV = "production"
